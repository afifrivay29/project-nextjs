import Heading from "../../components/Heading";
import { roboto_mono } from "../fonts";

export default function ContactPage() {
  return (
    <Heading className={roboto_mono.className}>Halaman Contact Us</Heading>
  );
}
